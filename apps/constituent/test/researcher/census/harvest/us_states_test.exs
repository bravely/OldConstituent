defmodule Researcher.Census.Harvest.UsStatesTest do
  use Constituent.DataCase

  describe "harvest_us_states/1" do
    test "it uses the correct shapefile"
    test "it creates the right areas"
    test "it creates the right number of areas"
    test "it labels them all as US States"
    test "it puts in the right codes"
  end
end
